{
    "__comment": "This is an example config file for the build system. Copy it to your Jekyll site root and name it _build_config.json.",

    "minify_options": "--extensions=html,htm,js --omit-filename-includes=.min.js",
    "git_remotes": [
        "origin"
    ]
}
